/**
 * Layout próprio para as telas já migradas ao tema "Japanese Modern"
 * (landing e /entrar). Sem o SiteFooter escuro do grupo (public) — cada
 * página aqui traz seu próprio rodapé/rodapé embutido no novo visual.
 */
export default function LandingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
