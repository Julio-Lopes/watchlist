[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@9.15.9 --activate",
  "pnpm i --frozen-lockfile"
]

[phases.build]
cmds = ["pnpm --filter @watchlist/api build"]